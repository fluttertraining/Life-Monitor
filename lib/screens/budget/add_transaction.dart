import 'package:flutter/material.dart';

class AddTransactionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Add Trasaction Screen'),
      ),
      body: Center(
        child: Text('Add Trasaction Screen'),
      ),
    );
  }
}
